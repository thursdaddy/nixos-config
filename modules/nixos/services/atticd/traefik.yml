http:
  routers:
    attic:
      rule: "Host(`attic.thurs.pw`)"
      service: attic
      entrypoints:
        - "websecure"
      tls:
        certResolver: letsencrypt

  services:
    attic:
      loadBalancer:
        servers:
          - url: http://host.docker.internal:5454
