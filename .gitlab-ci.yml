default:
  tags:
    - wormhole

stages:
  - flake

flake:update:
  stage: flake
  image: local/nix-daemon:latest
  before_script:
  - . "$HOME/.nix-profile/etc/profile.d/nix.sh"
  script:
    - nix --version
    - nix flake update
